Aplicación CRUD Java + IntelliJ IDEA + Oracle 19c:.
Procedimientos Almacenados + Trigger + JDBC:

<img width="1536" height="1024" alt="image" src="https://github.com/user-attachments/assets/771aa78c-a224-408f-b127-1baa1a347118" />  

<img width="2550" height="1079" alt="image" src="https://github.com/user-attachments/assets/e7bbcfcf-16b8-4c7c-ba25-0fd17915eefd" />    

```
El siguiente proyecto permite realizar operaciones CRUD utilizando:
Java
IntelliJ IDEA
JDBC
Oracle Database 19c
Procedimientos almacenados
Trigger automatico

Además:
El trigger detecta automáticamente cuando se inserta el registro número 50.
1. SCRIPT COMPLETO ORACLE 19c
Crear Tabla
CREATE TABLE EMPLEADOS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE VARCHAR2(100),
    CARGO VARCHAR2(100),
    SALARIO NUMBER(10,2),
    FECHA_REGISTRO DATE DEFAULT SYSDATE
);

2. TABLA LOG PARA EL TRIGGER
CREATE TABLE LOG_REGISTRO_50 (
    ID_LOG NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    MENSAJE VARCHAR2(300),
    FECHA_LOG DATE DEFAULT SYSDATE
);

3. PROCEDIMIENTOS ALMACENADOS CRUD
CREATE
CREATE OR REPLACE PROCEDURE SP_INSERTAR_EMPLEADO(
    P_NOMBRE IN VARCHAR2,
    P_CARGO IN VARCHAR2,
    P_SALARIO IN NUMBER
)
AS
BEGIN

    INSERT INTO EMPLEADOS(
        NOMBRE,
        CARGO,
        SALARIO
    )
    VALUES(
        P_NOMBRE,
        P_CARGO,
        P_SALARIO
    );

    COMMIT;

END;
/

READ
CREATE OR REPLACE PROCEDURE SP_LISTAR_EMPLEADOS(
    P_CURSOR OUT SYS_REFCURSOR
)
AS
BEGIN

    OPEN P_CURSOR FOR
    SELECT *
    FROM EMPLEADOS
    ORDER BY ID;

END;
/

UPDATE
CREATE OR REPLACE PROCEDURE SP_ACTUALIZAR_EMPLEADO(
    P_ID IN NUMBER,
    P_NOMBRE IN VARCHAR2,
    P_CARGO IN VARCHAR2,
    P_SALARIO IN NUMBER
)
AS
BEGIN

    UPDATE EMPLEADOS
    SET
        NOMBRE = P_NOMBRE,
        CARGO = P_CARGO,
        SALARIO = P_SALARIO
    WHERE ID = P_ID;

    COMMIT;

END;
/

DELETE
CREATE OR REPLACE PROCEDURE SP_ELIMINAR_EMPLEADO(
    P_ID IN NUMBER
)
AS
BEGIN

    DELETE FROM EMPLEADOS
    WHERE ID = P_ID;

    COMMIT;

END;
/

4. TRIGGER PARA DETECTAR EL REGISTRO NUMERO 50
CREATE OR REPLACE TRIGGER TRG_REGISTRO_50
AFTER INSERT ON EMPLEADOS
FOR EACH ROW

DECLARE
    V_TOTAL NUMBER;

BEGIN

    SELECT COUNT(*)
    INTO V_TOTAL
    FROM EMPLEADOS;

    IF V_TOTAL = 50 THEN

        INSERT INTO LOG_REGISTRO_50(MENSAJE)
        VALUES(
            'Se insertó el registro número 50 correctamente'
        );

    END IF;

END;
/

5. REGISTROS DE EJEMPLO
BEGIN

    FOR I IN 1..50 LOOP

        SP_INSERTAR_EMPLEADO(
            'Empleado ' || I,
            'Desarrollador',
            2000 + I
        );

    END LOOP;

END;
/

6. CONSULTAR EL LOG DEL TRIGGER
SELECT *
FROM LOG_REGISTRO_50;

7. PROYECTO JAVA EN INTELLIJ IDEA
Estructura
CRUDOracleJava
│
├── src
│   ├── conexion
│   │     ConexionOracle.java
│   │
│   ├── dao
│   │     EmpleadoDAO.java
│   │
│   └── Main.java
│
└── ojdbc8.jar

8. DEPENDENCIA JDBC ORACLE
Descargar
JDBC Driver Oracle 19c
Sitio Oficial
Oracle JDBC Driver Downloads
Agregar al Proyecto
ojdbc8.jar

9. CLASE DE CONEXIÓN
ConexionOracle.java
package conexion;

import java.sql.Connection;
import java.sql.DriverManager;

public class ConexionOracle {

    private static final String URL =
            "jdbc:oracle:thin:@localhost:1521:XE";

    private static final String USER = "SYSTEM";

    private static final String PASSWORD = "123456";

    public static Connection conectar() {

        Connection con = null;

        try {

            Class.forName(
                    "oracle.jdbc.driver.OracleDriver"
            );

            con = DriverManager.getConnection(
                    URL,
                    USER,
                    PASSWORD
            );

            System.out.println(
                    "Conexion exitosa Oracle"
            );

        } catch (Exception e) {

            System.out.println(
                    "Error conexion"
            );

            e.printStackTrace();

        }

        return con;
    }
}

10. DAO CRUD CON PROCEDIMIENTOS
EmpleadoDAO.java
package dao;

import conexion.ConexionOracle;

import java.sql.*;

public class EmpleadoDAO {

    Connection con;

    public EmpleadoDAO() {

        con = ConexionOracle.conectar();

    }

    // INSERTAR
    public void insertarEmpleado(
            String nombre,
            String cargo,
            double salario
    ) {

        try {

            CallableStatement cs =
                    con.prepareCall(
                            "{call SP_INSERTAR_EMPLEADO(?,?,?)}"
                    );

            cs.setString(1, nombre);
            cs.setString(2, cargo);
            cs.setDouble(3, salario);

            cs.execute();

            System.out.println(
                    "Empleado insertado"
            );

        } catch (Exception e) {

            e.printStackTrace();

        }
    }

    // LISTAR
    public void listarEmpleados() {

        try {

            CallableStatement cs =
                    con.prepareCall(
                            "{call SP_LISTAR_EMPLEADOS(?)}"
                    );

            cs.registerOutParameter(
                    1,
                    oracle.jdbc.OracleTypes.CURSOR
            );

            cs.execute();

            ResultSet rs =
                    (ResultSet) cs.getObject(1);

            while (rs.next()) {

                System.out.println(
                        rs.getInt("ID") + " - " +
                        rs.getString("NOMBRE") + " - " +
                        rs.getString("CARGO") + " - " +
                        rs.getDouble("SALARIO")
                );

            }

        } catch (Exception e) {

            e.printStackTrace();

        }
    }

    // ACTUALIZAR
    public void actualizarEmpleado(
            int id,
            String nombre,
            String cargo,
            double salario
    ) {

        try {

            CallableStatement cs =
                    con.prepareCall(
                            "{call SP_ACTUALIZAR_EMPLEADO(?,?,?,?)}"
                    );

            cs.setInt(1, id);
            cs.setString(2, nombre);
            cs.setString(3, cargo);
            cs.setDouble(4, salario);

            cs.execute();

            System.out.println(
                    "Empleado actualizado"
            );

        } catch (Exception e) {

            e.printStackTrace();

        }
    }

    // ELIMINAR
    public void eliminarEmpleado(int id) {

        try {

            CallableStatement cs =
                    con.prepareCall(
                            "{call SP_ELIMINAR_EMPLEADO(?)}"
                    );

            cs.setInt(1, id);

            cs.execute();

            System.out.println(
                    "Empleado eliminado"
            );

        } catch (Exception e) {

            e.printStackTrace();

        }
    }
}

11. CLASE PRINCIPAL
Main.java
import dao.EmpleadoDAO;

public class Main {

    public static void main(String[] args) {

        EmpleadoDAO dao =
                new EmpleadoDAO();

        // INSERTAR
        dao.insertarEmpleado(
                "Carlos",
                "Backend Developer",
                4500
        );

        // LISTAR
        dao.listarEmpleados();

        // ACTUALIZAR
        dao.actualizarEmpleado(
                1,
                "Carlos Actualizado",
                "Arquitecto Software",
                7000
        );

        // ELIMINAR
        dao.eliminarEmpleado(2);

        // LISTAR NUEVAMENTE
        dao.listarEmpleados();
    }
}

12. RESULTADO ESPERADO
Conexion exitosa Oracle

Empleado insertado
1 - Carlos - Backend Developer - 4500

Empleado actualizado
Empleado eliminado

13. VALIDAR TRIGGER
Después de insertar 50 registros:
SELECT *
FROM LOG_REGISTRO_50;

Resultado
ID_LOG | MENSAJE                                      | FECHA_LOG
1      | Se insertó el registro número 50 correctamente

14. TECNOLOGÍAS UTILIZADAS
Java 17
IntelliJ IDEA
JDBC
Oracle 19c
PL/SQL
Procedimientos almacenados
Triggers
Oracle CallableStatement

15. MEJORAS POSIBLES
Se puede ampliar con:
Swing GUI
JTable
Login Oracle
Maven
Hibernate
JPA
Exportar PDF
Exportar Excel
Reportes JasperReports
CRUD visual empresarial
Arquitectura MVC
API REST Spring Boot
Docker Oracle XE
Validaciones avanzadas
Auditoría completa con triggers

16. CONCLUSION
Este proyecto demuestra cómo integrar:
Oracle 19c
Java JDBC
Procedimientos almacenados
Triggers automaticos
Arquitectura DAO
:. . / .
